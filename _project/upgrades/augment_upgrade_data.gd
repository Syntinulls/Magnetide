extends UpgradeData
class_name AugmentUpgradeData

## Upgrade for an augment: its level cap + per-level costs, and (Phase 3c) effects targeting the
## variables its AugmentBehavior exposes. Until 3c wires those, effects stay empty and the behavior
## keeps its own per-level scalars.
