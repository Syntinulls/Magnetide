extends Node2D

## The speed at which the level scrolls (used by trash stream and foreground).
@export var level_speed: float = 300.0
## The Y position of the ocean surface level.
@export var surface_y: float = 500.0


func _ready() -> void:
	pass
